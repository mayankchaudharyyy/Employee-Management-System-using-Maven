# Employee Management System 🖥️

This project is a simple console-based Employee Management System developed using Java and Maven. It demonstrates fundamental DevOps practices such as build automation with Maven, version control using Git, and source code management using GitHub. The application provides a menu-driven interface that allows users to add employee details and view stored employee records.

Technologies used in this project include:
- Java  
- Maven  
- Git  
- GitHub  
- Visual Studio Code  

Project structure 📂:
employee-management  
├── pom.xml  
├── .gitignore  
└── src/main/java/com/devops/employee  
  ├── App.java  
  └── Employee.java  

The main features of the application include:
- Adding employee information such as ID, name, and salary  
- Viewing all stored employee records  
- Building and managing the project using Maven commands  

To run the project, follow these steps:

1. Clone the GitHub repository to your local system.
2. Navigate to the project directory.
3. Build the project using the Maven command:
   `mvn clean install`
4. Run the application using the following command:
   `java -cp target/employee-management-1.0-SNAPSHOT.jar com.devops.employee.App`


This project covers key DevOps concepts including version control systems, build automation, and collaborative workflows using Git and GitHub.
